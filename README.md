# No Attention Span

Automatically Open/Close TikTok during a long build

## Installation

### Archlinux

`yay -S no-attention-span`

## Usage

Just run nas `nas <command to build your project>` If you use cargo just do: `nas cargo build`, for npm: `nas npm run build`, ... you get it
